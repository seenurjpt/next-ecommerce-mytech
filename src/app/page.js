import Loader from "./components/Loader";

const Home = () => {
  return (
    <div className='flex justify-center items-cente h-full w-full'>
      <Loader />
    </div>
  );
};

export default Home;
